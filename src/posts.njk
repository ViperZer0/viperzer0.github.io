---
permalink: "posts/{{ pagination.items[0].data.id }}/index.html"
---
<!doctype html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="/bundle.css"/>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Newsreader"/>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inter"/>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inconsolata"/>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Fragment+Mono"/>
        <link rel="stylesheet" href="https://unpkg.com/prismjs@1.30.0/themes/prism-tomorrow.min.css"/>
        <title></title>
    </head>
    <body>
        {% include "navbar.njk" %}
        <div class="content-wrapper-outer">
        <div class="content-wrapper-inner">
        <main>
            <h1>{{ pagination.items[0].data.title }}</h1>
            {{ pagination.items[0].content | safe }}
        </main>
        </div>
        <nav aria-label="pagination-menu">
        <div class="content-footer">
            <div class="content-footer-previous-item">
                {% if pagination.href.previous %}
                <a class="content-footer" href="{{ pagination.href.previous }}">Previous Article</a> 
                <a class="content-footer" href="{{ pagination.href.previous }}">{{ pagination.page.previous.data.title }}</a>
                {% else %}
                <a class="content-footer" href="">No previous articles!</a>
                {% endif %}
            </div>
            <div class="content-footer-home-item">
                <a class="content-footer" href="/">Home</a> 
            </div>
            <div class="content-footer-next-item">
                {% if pagination.href.next %}
                <a class="content-footer" href="{{ pagination.href.next }}">Next Article</a> 
                <a class="content-footer" href="{{ pagination.href.next }}">{{ pagination.page.next.data.title }}</a>
                {% else %}
                <a class="content-footer" href="">No new articles!</a>
                {% endif %}
            </div>
        </div>
        </nav>
        </div>
        </div>
    </body>
</html>
